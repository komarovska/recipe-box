import React, { Component } from 'react';

import RecipesList from '../RecipesList/index.js';

export default class App extends Component {
  render() {
    return (
      <RecipesList />
    );
  }
}
